#include <iostream>
using namespace std;

void main()
{
	/*int select;
	cin >> select;
	switch (select)
	{
	case 1:
		cout << "콜라" << endl;
		break;
	case 2:
		cout << "사이다" << endl;
		break;
	case 3:
		cout << "커피" << endl;
	default:
		cout << "솔의눈" << endl;
		break;
	}*/

	// 문제) if문으로 두 수의 차를 구하는 프로그램
	/*int a, b, c;
	cin >> a >> b;

	if (a > b)
	{
		cout << a - b << endl;
	}
	else
	{
		cout << b - a << endl;
	}

	cout << "======================================" << endl;*/

	// 문제) 입력받은 정수가 양수인지 음수인지 0인지 판별하는 프로그램
	/*cin >> c;
	if (c > 0)
	{
		cout << "양수" << endl;
	}
	else if (c < 0)
	{
		cout << "음수" << endl;
	}
	else
	{
		cout << "0" << endl;
	}

	cout << "======================================" << endl;*/

	// 문제) 삼항 연산자를 이용하여 두 수의 차를 구하는 프로그램
	//cout << ((a > b) ? a - b : b - a) << endl;
}