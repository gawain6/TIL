#include <stdio.h>
#include <stdlib.h>

int* getPrime(int n);

int main() {
    int n, m;
    int *numPtr;
    scanf("%d %d", &n, &m);
    
    
        
}

int* getPrime(int n) {
    if(n<2) return 
}